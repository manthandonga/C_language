#include<stdio.h>
main()
{
	int base,height,area;
	
	printf("base of triangle is :");
	scanf("%d",&base);
	
	printf("height of triangle is :");
	scanf("%d",&height);
	
	area=(base*height)/2;
    printf("Area of triangle is =%d",area);
}
